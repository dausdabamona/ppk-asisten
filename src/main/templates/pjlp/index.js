/**
 * Placeholder untuk generator dokumen PJLP
 * 
 * Kategori: PJLP (Pegawai Tidak Tetap)
 * Generators: 0 (akan diimplementasikan fase berikutnya)
 * 
 * Planned generators:
 * - Kontrak PJLP (3-5 halaman)
 * - Nominatif Bulanan (1-2 halaman)
 * - Kwitansi PJLP (1 halaman)
 */

// TODO: Implementasi generator PJLP
// const { KontrakPJLPGenerator } = require('./kontrak-pjlp');
// const { NominatifPJLPGenerator } = require('./nominatif-pjlp');
// const { KwitansiPJLPGenerator } = require('./kwitansi-pjlp');

const pjlpGenerators = {
  // TODO: Register generators when implemented
  // 'KONTRAK_PJLP': new KontrakPJLPGenerator(),
  // 'NOMINATIF_PJLP': new NominatifPJLPGenerator(),
  // 'KWITANSI_PJLP': new KwitansiPJLPGenerator(),
};

module.exports = { pjlpGenerators };
